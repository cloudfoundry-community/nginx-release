---
name: fetcher
templates:
  ctl.sh: bin/ctl
  fetcher.sh.erb: bin/fetcher.sh

packages:

properties:
  fetcher.url:
    description: 'The URL to fetch'
  fetcher.output_path:
    description: 'The path to output the static file'
  fetcher.sleep:
    default: 10
    description: 'The time until the next retrieval (in seconds)'
